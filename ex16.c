#include<stdio.h>

void sum(int a, int b){
    int c = a +b;
    printf("\n sum is %d",c);
}

int main()
{
    sum(10,20);
    sum(20,20);
    sum(30,20);
    sum(40,20);
    sum(50,20);
    sum(60,20);
    sum(70,20);
    sum(80,20);
    return 0;
}
